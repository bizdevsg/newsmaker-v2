import type { Locale } from "@/locales";

export type StrategicItem = {
  key: string;
  eyebrow?: string;
  title: string;
  date: string;
  image: string;
  href?: string;
};

export type HomeInsightBoardsLocale = Locale;
